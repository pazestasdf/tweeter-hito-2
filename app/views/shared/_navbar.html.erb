<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">Tweeter</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
        
                <li class="nav-item">
                <%= link_to 'Explorar', all_tweets_path, class: 'nav-link'%>
                </li>
                <% if user_signed_in? %>
                    <% if current_user.admin%>
                    <li class="nav-item">
                        <%= link_to 'Admin', admin_root_path, class: 'nav-link'%>
                    </li>
                    <% end %>
                <li class="nav-item">
                    <a class="nav-link" href="<%= root_path %>">@<%= current_user.username %></a>
                </li>
                <li class="nav-item">
                    <%= link_to 'Cerrar Sesión', destroy_user_session_path, class: 'nav-link'%>
                </li>
                <% else %>
                <li class="nav-item">
                    <%= link_to 'Registro', new_user_registration_path, class: 'nav-link'%>
                </li>
                <li class="nav-item">
                    <%= link_to 'Login', new_user_session_path, class: 'nav-link'%>
                </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>